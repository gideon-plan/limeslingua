## limeslingua.nim -- Limes + Lingua multilingual vector search. Re-export module.

{.experimental: "strict_funcs".}

import limeslingua/[detect, normalize, route, search]
export detect, normalize, route, search
